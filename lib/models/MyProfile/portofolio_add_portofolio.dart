
import 'portofolio_add_portofolio_base.dart';
/** AUTOGENERATE OFF **/
class AddPortofolioPortofolioModel extends AddPortofolioPortofolioBase{

  Map<String, dynamic> json;
  AddPortofolioPortofolioModel(Map<String, dynamic> this.json):super(json);
  /*
  @override
  Map<String, dynamic> convertFormDataAction(var data){
    String image = '';
    if (model.model.image!= null) {
      if (model.model.image.temp != null) {
        image = '{"status":"1","name":"${model.model.image.name}","temp":"${model.model.image.temp}"}';
      }
    }

    var formData = {
      "portofolio[_trigger_]":"",
      "portofolio[year]": "${model.model.year }",
      "portofolio[description]": "${model.model.description }",
      "portofolio[link]": "${model.model.link }",
      "portofolio[image]": '$image',
      "portofolio[image_lastval]": '{"name":"${model.model.image.name}","dir":"${model.model.image.dir}","file":"${model.model.image.file}","thumb":"${model.model.image.thumb}"}',

    };


    return( formData);
  }

   */

}













  

